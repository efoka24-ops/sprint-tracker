# Postgres (Neon, Supabase, Vercel Postgres...) — pooled connection
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"
# Mot de passe unique pour l'espace Tech Lead (validation vendredi / admin)
ADMIN_PASSWORD="change-moi"
